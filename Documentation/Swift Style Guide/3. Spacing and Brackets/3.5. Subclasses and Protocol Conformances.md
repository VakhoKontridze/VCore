# Subclasses and Protocol Conformances

Subclasses and protocol conformances should be spaced out with a single space.

Not Preferred:

```swift
struct SomeStruct : SomeProtocol {
    ...
}
```

Preferred:

```swift
struct SomeStruct: SomeProtocol {
    ...
}
```
